import { MateriasDirective } from './materias.directive';

describe('MateriasDirective', () => {
  it('should create an instance', () => {
    const directive = new MateriasDirective();
    expect(directive).toBeTruthy();
  });
});
